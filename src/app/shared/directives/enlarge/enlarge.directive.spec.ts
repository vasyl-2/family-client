import { EnlargeDirective } from './enlarge.directive';

describe('EnlargeDirective', () => {
  it('should create an instance', () => {
    const directive = new EnlargeDirective();
    expect(directive).toBeTruthy();
  });
});
